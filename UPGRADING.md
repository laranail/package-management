# Upgrading

`laranail/package-management` is pre-1.0 — there are no prior stable versions to upgrade from.
Breaking changes (to the `Extensions` facade, the `Contracts\*` interfaces, or the manifest schemas in
[docs/manifests.md](docs/manifests.md)) will be documented here per release, with a clear before/after.
